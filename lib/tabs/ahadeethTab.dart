import 'package:flutter/material.dart';

class Ahadeeth extends StatelessWidget {
  const Ahadeeth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}